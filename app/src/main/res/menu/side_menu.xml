<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <group>
        <item android:id="@+id/nav_reservation"
            android:icon="@drawable/ic_reservation"
            android:title="Make Reservation"
            tools:ignore="HardcodedText" />
        <item android:id="@+id/nav_show_reservation"
            android:icon="@drawable/ic_show_reservations"
            android:title="Show Reservations"
            tools:ignore="HardcodedText" />
    </group>

    <item android:title="Profile"
        tools:ignore="HardcodedText">
        <menu>
            <item android:id="@+id/nav_profile"
                android:icon="@drawable/ic_profile"
                android:title="Profile" />
            <item android:id="@+id/nav_logout"
                android:icon="@drawable/ic_logout"
                android:title="Logout" />
        </menu>
    </item>
</menu>
